spring:
  application:
    name: playground-webflux
  webflux:
    base-path: /api
  server:
    port: 8080
  springdoc:
    swagger-ui:
      path: /swagger-ui.html
      enabled: true
    api-docs:
      path: /v3/api-docs